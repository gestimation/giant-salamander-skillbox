# OpenAI申請用ロゴ

採用案は `logo-a-palette-2-navy-jade`。

- ネイビー：`#172A3A`
- ジェイド：`#63A083`
- 背景色：`#F4F6F3`

`final/` に申請・表示確認用の1024 px版と512 px版を格納する。通常は背景付き1024 px版を使用し、透過画像が必要な媒体では透過版を使用する。

比較用の原案と5つの配色案は、このフォルダ直下に保持する。生成・透過処理の中間ファイルは `working/` に保持する。
